"""Platzhalter — wird im Rahmen von Karte #316 gefüllt.

Der Router ist bereits in `app/main.py` registriert und das Menü verweist
schon hierher, damit die vier Seiten unabhängig voneinander gebaut werden
können, ohne sich an denselben Dateien zu überschneiden.
"""

from fastapi import APIRouter

router = APIRouter()
